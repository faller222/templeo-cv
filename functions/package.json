{
  "name": "templeo-cv-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "20 || 22" },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "express": "^4.21.2",
    "firebase-functions": "^6.3.0",
    "jose": "^6.0.11"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "~5.8.2"
  }
}
